/// <reference types="vite/client" />

/*
 * Vite's own module declarations, so `import mark from '@/assets/logo-mark.svg'`
 * is a string in TypeScript rather than "cannot find module". It also gives the
 * service-worker and import-meta-env types the app uses in main.tsx.
 */
